To run this test case the user must replace

setenv  NUCBAS /dev/null

in the rungms script with

setenv  NUCBAS 'USER-GMS'/qmnuc/neotests/READBFIN/nucbasis
        where 'USER-GMS' is the users GAMESS path
